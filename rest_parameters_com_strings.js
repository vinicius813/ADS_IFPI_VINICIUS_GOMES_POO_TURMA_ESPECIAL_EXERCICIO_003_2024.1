function exibicao(elementos) {
    elementos.forEach(function (elemento_unico) {
        console.log(elemento_unico);
    });
}
exibicao("a", "b");
exibicao("a", "b", "c");
exibicao("a", "b", "c", "d");
